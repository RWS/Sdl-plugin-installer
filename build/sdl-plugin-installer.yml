trigger:
  branches:
    include:
    - master

pr:
  branches:
    include:
    - master

pool:
  vmImage: '$(DefaultBuildVm)'

variables:
- name: BuildPlatform
  value: 'mixed platforms'
- name: Configuration
  value: 'release'

name: $(Build.DefinitionName)_$(SourceBranchName)_$(Version.MajorMinorPatch).$(Version.Revision)

steps:
- task: NuGetToolInstaller@1
- task: NuGetCommand@2
  inputs:
    command: 'restore'
    restoreSolution: '**/*.sln'
    feedsToUse: 'select'

- task: SDLVersion@2
  inputs:
    Path: '$(Build.SourcesDirectory)'
    VersionNumber: '$(BuildDefinitionName)_$(Version.MajorMinorPatch).$(Version.Revision)'
    FilenamePattern: '**/*info.cs'

- task: VSBuild@1
  displayName: 'Build solution Deployment/Sdl Plugin Installer/Sdl.Community.SdlPluginInstaller.sln'
  inputs:
    solution: '**/Sdl.Community.SdlPluginInstaller.sln'
    msbuildArgs: '/p:RunWixToolsOutOfProc=true'
    platform: '$(BuildPlatform)'
    configuration: '$(Configuration)'

- task: NuGetCommand@2
  condition: ne(variables['Build.Reason'], 'PullRequest')
  inputs:
    command: 'pack'
    packagesToPack: '**/*.nuspec'
    versioningScheme: 'byBuildNumber'